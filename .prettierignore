# Not ours to format.
node_modules/
web/.next/
build/
**/build/
.gradle/
.kotlin/
web/pnpm-lock.yaml
web/test-results/
web/playwright-report/

# Byte-compared fixtures. These are regression goldens a test diffs verbatim, so reflowing them
# rewrites the thing under test.
analyzer/probe/testdata/
control-plane/src/test/resources/atrail/
# The Go control-plane's goldens, for the same reason. gocp/internal/conformance/wire_json_test.go's
# assertWireBytes "deliberately does not normalise, re-indent or semantically compare" — a reflow would
# rewrite the contract it exists to pin, and the suite would pass against the reflowed bytes while the
# wire shape it was guarding drifted unnoticed.
gocp/internal/conformance/testdata/
gocp/internal/authz/testdata/

# Generated by release-please, which emits `*` bullets and unwrapped lines. Reformatting them to this
# repo's style would be undone on the next release, and the mismatch fails `verify` on every release PR.
CHANGELOG.md
**/CHANGELOG.md
